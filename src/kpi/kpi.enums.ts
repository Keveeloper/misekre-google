/** Módulos monitoreados por los KPIs. */
export enum KpiModule {
  CALENDAR = 'CALENDAR',
  FINANCIAL_CONTROL = 'FINANCIAL_CONTROL',
  TASKS = 'TASKS',
}

/** Resultado de una operación del agente. */
export enum KpiStatus {
  SUCCESS = 'SUCCESS',
  FAILURE = 'FAILURE',
}
